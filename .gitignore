# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Data files (keep in separate repo/location — never commit raw data or secrets)
data/*.csv
data/*.xlsx
data/*.xls
!data/mceRealData.js

# History snapshots — stay local only (written by weekly-update.sh)
data/history/

# Org62 contracts Excel files (contain customer data — never commit)
Contracts_*.xlsx
contracts_*.xlsx
